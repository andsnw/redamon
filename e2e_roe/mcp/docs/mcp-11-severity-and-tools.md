# Rules of Engagement — Aperture Science

## Reporting

Only **critical and high** severity findings are in scope.

## Tool prohibitions

The following are explicitly forbidden on this engagement:

- `execute_hydra` — no credential attacks of any kind
- `metasploit_console` — no exploitation framework

Name them by their exact tool identifiers so the prohibition is enforced rather
than merely noted.

## Rate

Maximum **7 requests per second**.
